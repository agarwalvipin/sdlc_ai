# Phase 5: Execution Plan

## Milestones
### Epic 1: Dependencies & Config
- [ ] Install `python-jose`, `passlib`, `bcrypt`.
- [ ] Add `SECRET_KEY` config.

### Epic 2: Core Auth Logic
- [ ] Implement `verify_password` and `get_password_hash` utils.
- [ ] Implement `create_access_token`.

### Epic 3: API Endpoints
- [ ] Add `/token` endpoint (Login).
- [ ] Add `/users/me` endpoint (Protected).
