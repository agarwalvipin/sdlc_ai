name: Validate Schemas

on:
  push:
    branches: [ main ]
    paths:
      - 'architecture/**/*.json'
      - 'schemas/**/*.json'
  pull_request:
    branches: [ main ]
    paths:
      - 'architecture/**/*.json'
      - 'schemas/**/*.json'

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16'

      - name: Install AJV CLI
        run: npm install -g ajv-cli

      - name: Validate Phase 0 (Triage)
        run: |
          if [ -f architecture/00-triage.json ]; then
            ajv validate -s schemas/triage-decision.schema.json -d architecture/00-triage.json
          fi

      - name: Validate Phase 1 (Context)
        run: |
          if [ -f architecture/01-context.json ]; then
            ajv validate -s schemas/context-spec.schema.json -d architecture/01-context.json
          fi

      - name: Validate Phase 2 (Design)
        run: |
          if [ -f architecture/02-design.json ]; then
            ajv validate -s schemas/design-spec.schema.json -d architecture/02-design.json
          fi

      - name: Validate Phase 3 (Security)
        run: |
          if [ -f architecture/03-security.json ]; then
            ajv validate -s schemas/security-report.schema.json -d architecture/03-security.json
          fi

      - name: Validate Phase 4 (Decision)
        run: |
          if [ -f architecture/04-decision.json ]; then
            ajv validate -s schemas/decision-record.schema.json -d architecture/04-decision.json
          fi

      - name: Validate Phase 5 (Plan)
        run: |
          if [ -f architecture/05-plan.json ]; then
            ajv validate -s schemas/execution-plan.schema.json -d architecture/05-plan.json
          fi
